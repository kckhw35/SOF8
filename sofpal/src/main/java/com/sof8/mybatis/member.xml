<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sof8.mapper.MemberMapper">
	<!-- 회원가입 -->
	<insert id="insert" parameterType="Member">
		INSERT INTO member (user_id, pwd, name, email, tel, postcode, addr, de_addr) 
		VALUES (#{user_id}, #{pwd}, #{name}, #{email}, #{tel}, #{postcode}, #{addr}, #{de_addr})
	</insert>
	<select id="select" parameterType="String" resultType="Member">
		SELECT * FROM member WHERE user_id = #{user_id}
	</select>
	<select id="selectall" resultType="Member">
		SELECT * FROM member
	</select>
	<!-- 아이디 찾기 -->
	<select id="selectMemberId" parameterType="Member" resultType="Member">
		SELECT * FROM member WHERE name = #{name} AND email=#{email}
	</select>	
	<!-- 회원정보 수정 -->
	<update id="update" parameterType="Member">
		UPDATE member 
		SET pwd = #{pwd}, name = #{name}, email = #{email}, tel = #{tel},
		 	postcode = #{postcode}, addr = #{addr}, de_addr = #{de_addr} 
		WHERE user_id = #{user_id}
	</update>
	<!-- 회원탈퇴 시 휴면상태로 변경 -->
	<update id="updateEnable" parameterType="String">
		UPDATE member SET enable = 0 WHERE user_id = #{user_id}
	</update>	
	<delete id="delete" parameterType="String">
		DELETE FROM member WHERE user_id = #{user_id}
	</delete>
</mapper>